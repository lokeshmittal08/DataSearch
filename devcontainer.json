{
    "name": "My Dev Container",
    "dockerFile": "Dockerfile",
    "mounts": [
        "source=${localWorkspaceFolder},target=/app,type=bind"
    ],
    "runArgs": [
        "--network=host"
    ]
}